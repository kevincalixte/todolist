import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    input: {
        borderWidth: 1,
        borderColor: 'red',
        padding: 10,
        marginHorizontal: 20,
        marginVertical: 10,
        borderRadius: 5,
    }
})

export default style;